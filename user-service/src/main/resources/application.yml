spring:
  application:
    name: user-service
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        import-check:
          enabled: false
      discovery:
        enabled: false
        register-enabled: false
    service-registry:
      auto-registration:
        enabled: false
